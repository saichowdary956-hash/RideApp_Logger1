# Environment Variables for RideApp_Logger1

# Server Configuration
PORT=3000

# Note: When deploying to Render, the PORT variable is automatically set by the platform
# You don't need to manually configure it in Render's dashboard